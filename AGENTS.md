# Project Agent Workflow

## 지침 문서 관리

- 루트 `AGENTS.md`는 공통 핵심 규칙과 필요한 문서의 안내만 담아 짧게 유지합니다.
- 특정 디렉터리에만 적용되는 규칙은 해당 디렉터리의 `AGENTS.md`로, 긴 절차와 참고 자료는 별도 문서로 분리합니다.
- 별도 문서로 분리할 때는 이 파일에 상대 경로와 읽어야 하는 조건을 명시합니다. 같은 규칙을 여러 문서에 중복하지 않습니다.
- 길이뿐 아니라 적용 범위와 반복을 기준으로 분리를 판단하며, 짧고 밀접한 규칙은 함께 유지합니다.
- 개발 완료 시에는 [개발 기록 기준](docs/development-records.md)을 읽습니다. 사용자·운영자에게 보이는 동작, 계약 또는 설정이 바뀌면 해당 문서를 함께 갱신하고, 작업별 영구 기록을 남깁니다.
- 새 작업을 시작하거나 이어받을 때는 [구조 안내](docs/architecture.md), 활성 항목이 있는 [작업 등록부](docs/worktree-tasks.md), 해당 작업의 개발 기록을 먼저 확인합니다.

## 언어

- 사용자 응답, README, 사용자 문서, UI 문구, PR 설명 등 사람에게 전달하는 산출물은 기본적으로 한국어를 사용합니다.
- 코드, 식별자, 변수명, API 이름, 명령어, 기술 용어는 기존 관례를 따르며 필요한 경우 영어를 유지합니다.
- 사용자가 다른 언어를 명시적으로 요청하면 그 요청을 따릅니다.

## 워크트리 기반 개발

- 개발 작업을 분배하거나 워크트리를 생성·검증·통합·정리하기 전에 [워크트리 운영 절차](docs/worktree-workflow.md)를 읽습니다.
- 기본 supervisor(Sol)는 기준 저장소에서 작업을 조율·통합하고, Luna는 작업별 형제 폴더 `../jusik-<slug>`와 전용 브랜치에서 개발합니다. 작업 하나당 워크트리와 구현 담당자를 하나씩 배정합니다. Astra는 해결되지 않은 중대 진단에만 제한된 읽기 전용 escalation으로 사용합니다.
- 실행 환경과 변경 가능한 데이터도 작업별로 분리합니다. 구체적인 격리 기준과 작업 기록 양식은 운영 절차를 따릅니다.
- 사용자가 종료 범위를 따로 지정하지 않으면 개발 완료에 로컬 `main` 병합, 통합 검증, handoff 저장까지 포함합니다. 완료 절차는 운영 문서를 따릅니다.

## 지속 개발 실행기

- 자동 실행기를 설치·운영하거나 수동 개발을 시작하기 전에는 [지속 개발 운영 절차](docs/development-runner.md)를 읽고 자동·수동 작업이 같은 저장소를 동시에 변경하지 않도록 합니다.
- 연구를 계획하거나 기존 큐 작업을 재개할 때는 [현재 연구 위임 조건](docs/research-mandate.md)과 해당 JSON을 먼저 읽습니다. 새 연구에는 최신 조건을 적용하고, 과거 실험의 재현에는 당시 고정 조건을 유지합니다.

## Agent 도구

- agent 역할·모델 라우팅·압축 보고·보조 도구와 supervisor 스킬의 상세 규칙은 [agent tooling 운영 문서](docs/agent-tooling.md)입니다. agent 라우팅이나 도구를 설정하거나 supervisor 작업을 시작하기 전에 읽습니다.
- 최신 프로젝트 역할표가 오래된 개인 supervisor skill의 모델 매핑보다 우선합니다. 기본 supervisor는 Sol이며, Astra는 제한된 읽기 전용 중대 진단 예외입니다.

## 연구 성과 공개

- 포트폴리오 연구 결과를 웹에 공개하거나 정정할 때는 [진행 화면 성과 공개 절차](docs/research-progress-publication.md)를 읽고 같은 조건의 비교 catalog를 함께 갱신합니다. 성과 비교가 없는 개발 작업에는 수치를 만들지 않습니다.

## Agent workflow

실제 spawn CLI가 role 필드를 제공하지 않는 경우에는 [agent routing compatibility procedure](docs/agent-tooling.md#roleless-cli-routing)를 따르고 `backend/jusik/agent_routing.py`의 model-only adapter를 사용합니다. 이 예외는 roleless CLI에만 적용하며 native interactive helper의 검사는 바꾸지 않습니다.

### 감독과 작업자 구성

- 주 supervisor(Sol)가 감독을 맡습니다. 연구 가설, 우선순위, 작업 범위, 완료 기준을 정하고 결과 통합과 최종 판단을 담당합니다.
- 작업자는 주 agent를 제외하고 최대 4개로 제한합니다. 하위 agent가 추가로 위임한 작업자도 이 한도에 포함합니다.
- 각 워크트리의 개발은 Luna가 담당합니다. 조사·계획·검증은 작업 난도와 역할별 모델 설정에 맞춰 배정합니다.
- 4개는 상한이며 항상 모두 실행할 필요는 없습니다. 독립적으로 수행할 구체적인 작업이 있을 때만 투입합니다.
- 각 작업에는 목표, 입력, 담당 범위, 산출물, 검증 방법, 종료 조건을 명시합니다. 결과는 근거와 함께 요약하여 감독에게 전달합니다.

작업자의 책임은 다음과 같이 나눕니다. 아래 역할은 작업 분담 기준이며, 별도의 agent를 모두 동시에 생성하라는 의미는 아닙니다.

1. **조사:** 관련 코드, 데이터 출처, 데이터 흐름, 기존 구현과 관례를 확인합니다.
2. **실험:** 사전에 정한 조건으로 연구를 실행하고 결과와 실패 원인을 정리합니다.
3. **구현:** 확정된 계획에 따라 코드를 수정합니다. 작업별 구현 소유자는 한 명만 둡니다.
4. **검증:** 계산 오류, 미래 데이터 누출, 회귀와 검증 누락을 독립적으로 확인합니다.

### 실행 순서와 병렬화

중간 이상 규모의 기능 작업과 버그 수정에는 프로젝트 전용 custom agent를 사용합니다.

1. `explore`로 관련 코드, 데이터 흐름, 관례, 테스트를 조사합니다.
2. 조사 결과를 `plan`에 전달합니다. 범위가 제한된 계획과 완료 기준을 받습니다.
3. 확정한 계획을 작업별 워크트리의 Luna에 전달합니다. `code` 역할이 없으면 기대 모델·추론 수준과 `fork_turns=none`을 명시한 `agent_type=default` worker를 사용합니다. 작업별 구현 소유자는 한 명만 둡니다.
4. 구현과 검증 후 Sol 기반 `review`를 실행합니다. 중요한 지적은 해당 Luna에 전달해 수정하고 diff를 다시 검토합니다. 일반 코드 문제는 서로 다른 가설이 두 번 실패한 뒤에 Astra escalation을 한 번 수행하며, 명백히 복잡한 금융 계산·미래 데이터 누출·설계 충돌의 중대 진단은 감독 근거를 남기고 처음부터 한 번 선택할 수 있습니다. 외부 근거가 없으면 자료 출처 대안이나 독립 작업으로 전환합니다.

의존하는 단계를 병렬로 실행하지 않습니다. 독립적인 읽기 전용 조사와 검토는 병렬화할 수 있습니다. 실험과 검증 실행은 입력과 조건이 확정되고, 다른 작업과 출력 경로 및 변경 가능한 상태가 겹치지 않을 때만 병렬화합니다. 같은 코드나 공유 운영 데이터를 여러 작업자가 동시에 수정하지 않습니다.

주 supervisor가 사용자와 소통하고, 의견 차이를 정리하며, 최종 결과를 반환합니다. 거래 연구 결과는 agent 간 동의가 아니라 독립된 데이터와 테스트를 통한 재현성으로 판단합니다. 작업 분담의 효율은 총 소요 시간, 확인 가능한 토큰 사용량, 재작업 횟수, 검증 통과 여부로 평가합니다.
