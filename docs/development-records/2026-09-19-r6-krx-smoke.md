# KRX 1년 수집 스모크 및 무거래 행 경계 보완

- 상태: 차단
- 기록 시각: 2026-09-18T16:50:00Z
- 작업 slug: `r6-krx-smoke`
- 기준/통합: `b419b91` / 진행 중
- 범위: KRX 공식 응답의 무거래 행을 안전하게 정규화하고, 2025-09-11~2026-09-11 한국 시장 근사 파일럿의 수집·readiness 게이트를 확인했습니다. 전략·주문·PAPER 설정은 변경하지 않았습니다.

## 변경과 결정

- `backend/jusik/market_data_collector.py`에서 KRX가 거래정지·무거래 종목의 OHLC/거래량을 `-` 또는 0으로 반환하는 경우 회원자격은 유지하고 일봉은 생성하지 않도록 했습니다. 알 수 없는 문자열은 계속 파서 오류로 처리합니다.
- `backend/tests/test_market_data_collector.py`에 0값 무거래 행 회귀 테스트를 추가했습니다.
- KRX 2025-09-11 단일 응답은 HTTP 200, 960행이었고 31행에서 open/high/low/volume이 0이었습니다. 수정 후 `universe=960`, `bars=929`로 정규화됐습니다.

## 문서·계약 영향

- 사용자 문서: 해당 없음. 사용자에게 보이는 계약은 바뀌지 않고, 잘못된 가격 생성을 방지하는 내부 정규화 경계만 보완했습니다.
- 운영 문서: `docs/worktree-tasks.md`에 본 작업의 수집 결과와 차단 조건을 등록해야 합니다.
- API·설정·데이터 계약: KRX 무거래 표시(`-`/0)를 기존 무거래 보존 정책에 명시적으로 포함했습니다.

## 검증

- `backend/.venv/bin/python -m pytest -q backend/tests/test_market_data_collector.py` — 129 passed.
- KRX 단일 응답 파싱 — `universe=960`, `bars=929`, `listed=960`.
- 1년 bounded collection (`request-budget=532`, `--resume`) — collected, cache files 534, excluded symbols 없음.
- 같은 cache와 실제 prepared output 경로를 사용한 `collect-status` — `completed=true`, `ready=true`, credentials missing 없음(exit 0). 별도 marker 출력 경로를 사용하면 output identity가 달라져 false가 되는 것을 확인했습니다.
- approximate pilot run — `status=insufficient`, `completeness=incomplete`, `readiness.ready=false`; trades/equity/metrics 없음. actions는 approximate/unknown이므로 성과로 승격하지 않았습니다.
- 실행하지 않은 검사: strict PIT 경제성·walk-forward·stress·PAPER 승격은 필수 기업행사·PIT membership 증거가 없어 수행하지 않았습니다.

## 안전·운영 상태

- 네트워크는 공식 KRX 읽기 전용 수집에만 사용했습니다. 실주문·브로커 API·PAPER 설정·운영 DB·원격 push는 변경하지 않았습니다.
- `.env`와 인증값은 기록·artifact에 포함하지 않았습니다.

## 증거와 재개

- audit: `/home/kwl/.local/share/jusik/portfolio-audit/20260919-r6-krx-smoke/`; `kr-pilot.json` SHA `d4448db8d418e45482589a1b39b6d4885992c31a58e2997357d8501b07a46352`; `run.json` SHA `652fa3985d7990143e77800210114d8533ece96a8e3b45e800c2a55f6ab9e64d`.
- 남은 작업·차단 조건: KRX 전체 기간의 PIT 기업행사·배당·상폐 및 관측 시각 근거가 없어 approximate 결과를 경제 성과로 사용할 수 없습니다. 해당 증거가 확보되면 동일 입력 해시·정책 해시를 확인한 뒤 별도 strict 게이트를 실행합니다.
- 다음 시작: `docs/worktree-tasks.md`의 `r6-krx-smoke` 상태와 runner 최신 planner 결과를 읽고, 추가 근거가 없으면 다음 독립 데이터 계약 슬라이스를 선택합니다.
