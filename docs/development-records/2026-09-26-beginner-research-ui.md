# 초보자를 위한 연구 화면 재구성

- 상태: 로컬 main 통합·검증 완료. 새 화면은 3337 포트에서 확인할 수 있으며 기존 3000 포트의 교체는 보류했습니다.
- 기록 시각: 2026-09-26T05:32:51Z
- 작업 slug: `beginner-research-ui`
- 기준/통합: `da4bf02` / `a1da784979228e23648faebd7a0c1377a07098f2`. 구현 `a854af0`, 검토 보완 `6657454`; 병합 직전 main은 `96b2018`입니다.
- 범위: 연구 탐색 메뉴, 개요, 결과, 모의 관찰, 선택형 개별 종목 도구의 안내와 오타 경로 호환을 수정했습니다. API, 계산, 연구 catalog, 전략, 거래 설정은 보존했습니다.

## 변경과 결정

- `frontend/app/research/research-navigation.tsx`: 주 메뉴를 세 가지로 줄이고 기존 경로를 기본 HTML 펼침 메뉴에 보존했습니다. 알려진 경로를 먼저 판별해 시장자료와 연구 도구가 동시에 활성화되지 않게 했습니다.
- `frontend/app/research/page.tsx`: 대표 비교 하나의 결론, 신뢰 한계, 다음 확인 질문, 비교 수치 순서로 재구성했습니다. 대표 지정이 없으면 임의 선택하지 않습니다. 연구 식별 확인 후 Decimal 비교로 수익·현금·위험·비용의 동반 변화를 설명합니다. 전체 목표와 조건은 접힌 영역에 남겼습니다.
- `frontend/app/research/progress/page.tsx`: 대표 비교를 목록 앞에 배치했습니다. 각 연구의 질문·결론·주요 한계·보고서 링크를 유지하며 규칙과 전체 비교 표를 접었습니다. 운영 상세도 접힌 영역에 유지합니다.
- `frontend/app/research/forward/page.tsx`: 모의 계좌와 연구의 차이를 설명하고 실제 상태·기록 시각·검증 불일치를 앞에 표시합니다. 입력·달력·기업행동의 차단은 숨기지 않고, 기술 진단과 원장은 펼쳐서 확인합니다. 가상 체결 완료를 투자 검증 완료로 표현하지 않습니다.
- `frontend/app/investor/page.tsx`, `frontend/app/invester/page.tsx`: 종목 탐색과 투자 메모는 선택 도구임을 설명하고 연구 화면 복귀 및 오타 경로 이동을 제공합니다. 조회 폼과 저장 기능은 변경하지 않았습니다.
- 독립 검토에서 모바일의 다음 확인 질문이 수치 아래로 밀리는 점을 확인해 결론 카드 바로 아래로 옮겼습니다. 긴 연구별 한계는 같은 위치의 펼침 영역에 보존했습니다.
- 화면은 제한된 청록 강조색, 밝은 배경, 짧은 결론, 나란한 수치, 기본 HTML 펼침 요소를 사용합니다. 신규 의존성은 없습니다.

## 문서·계약 영향

- 사용자 문서: `docs/investor-web-design.md`에 현재 정보 구조, 오류 분기, 선택 도구의 역할, 실행 경계를 추가했습니다.
- 운영 문서: 설정과 서비스 동작은 변경하지 않아 갱신하지 않았습니다.
- API·설정·데이터 계약: 변경 없음. 기존 응답 검증과 연구 식별 가드를 사용합니다.

## 검증

- `cd frontend && npm run lint` — 통과.
- `cd frontend && npm run typecheck` — 통과.
- `cd frontend && npm run build` — 통과. `/invester`를 포함한 기존·신규 경로가 생성됩니다.
- 독립 브라우저 초기 검사: supervisor가 1440px·390px의 주요 5개 경로에서 가로 넘침·콘솔 오류 없음, 주 메뉴 3개·현재 링크 1개, 본문 건너뛰기·기본 펼침 요소의 키보드 접근을 확인했습니다. 11초 자동 갱신 뒤 펼침 상태와 포커스를 유지했습니다.
- 최종 화면 크기: 동일한 1440px 폭에서 개요는 4,291px에서 2,151px, 연구 결과는 5,298px에서 3,077px로 줄었습니다. 390px 폭에서 개요는 6,703px에서 2,925px로 줄었습니다. 길이는 정보 계층의 변화만 설명하며 이해도 시험 결과가 아닙니다.
- 독립 상태 검사: empty·unavailable·invalid·malformed·offline·no-featured에서 잘못된 수익 표시 없이 상태를 구분했습니다. 설명 식별 불일치에서는 수치를 유지하고 알려진 결론을 숨겼습니다. 모의 관찰 일부 응답·전체 오류·검증 등록 오류가 표시됩니다.
- 최종 상태 검사 15개: 위 분기에 더해 모의 검증 대상 일치·운용 상태만 실패·동일 수치·반대 방향 수치도 확인했습니다. 불리한 변화와 근거 불일치를 숨기지 않았습니다.
- 최종 브라우저 검사: 주요 5개 경로를 1440px·390px에서 확인했습니다. 연구 상세 경로 6개는 HTTP 200이고 현재 메뉴가 각각 하나입니다. 모든 상세 영역을 펼쳐도 가로 넘침이 없으며 보고서 링크·조회 폼·필드·서버 액션이 보존됐습니다. `/invester`는 브라우저에서 `/investor`로 이동합니다.
- 개요의 실제 ‘모든 비교 보기’ 링크는 해당 연구 카드로 이동합니다. 주소를 직접 입력하는 초기 hash 이동은 기존 3000 화면과 새 화면 모두 자동 스크롤되지 않는 기존 동작으로 별도 기록했습니다. 이 작업에서 공통 라우터 동작을 변경하지 않았습니다.
- 별도 Sol review의 P2(다음 질문이 수치 뒤에 위치)를 원 구현자가 수정했습니다. `6657454` 재검토에서 필수 수정 없음. explore·plan·code·review의 routing 사전·사후 감사도 통과했습니다.
- 로컬 main에서 lint·typecheck·build를 다시 통과했습니다. 후속 타 작업 통합 커밋 `6739c63`에도 frontend 차이가 없음을 대조했습니다. main 빌드와 실제 연구 API를 연결한 3337의 개요·연구 결과·모의 관찰은 HTTP 200, 올바른 메뉴, 가로 넘침 없음입니다.
- 백엔드 검사는 실행하지 않았습니다. 백엔드 코드·계산·계약은 변경하지 않았습니다.
- 실제 일반 사용자 시험과 스크린리더 사용자 시험은 수행하지 않았습니다.

## 안전·운영 상태

- 실제 주문·broker API 테스트, 연구 운영 데이터 변경, 기존 서비스 재시작, 원격 push는 수행하지 않았습니다. 검증은 읽기 전용 공개 API 응답의 격리 복사본으로 진행했습니다.
- 기존에 중지된 runner에 idempotent pause를 실행하고 service inactive를 확인했습니다. 자동 개발 실행기를 재개하지 않았으며 timer는 active 상태를 보존했습니다.
- 기존 3000 포트 프런트엔드는 `start.sh:159,219`의 종료 처리로 인해 종료 시 두 백엔드도 함께 중지됩니다. 기존 3000·8000·8001 프로세스는 유지하고, main 빌드로 127.0.0.1:3337의 새 미리보기만 실행했습니다. 미리보기는 실제 연구 API를 읽으며 원래 기능을 그대로 제공합니다. 기존 3000의 교체는 서비스 중단 영향에 대한 사용자 확인 후 수행합니다.
- fixture 8337과 작업별 프런트 프로세스는 종료했습니다. 산출물의 SHA-256을 보존·확인한 뒤 병합된 전용 worktree와 브랜치를 정상 제거했습니다.

## 증거와 재개

- audit: `/home/kwl/.local/share/jusik/ui-redesign-20260926/`. `verification.json`은 검사 결과, `browser-layout-final.json`·`browser-states-final.json`·`browser-interactions-final.json`은 일반 검사, `browser-overview-followup.json`과 `final-overview-*`는 검토 보완 뒤 최종 개요, `main-preview-smoke.json`은 통합 빌드 확인입니다. `preservation.json`·`implementation.patch`·`manifest.json`으로 보존 범위와 파일 무결성을 확인할 수 있습니다.
- handoff: 같은 audit 디렉터리의 `HANDOFF.md`. 기존 사용자 소유 루트 `HANDOFF.md`와 다른 작업의 변경·worktree는 보존했습니다.
- 남은 작업: 기존 3000 포트 교체 여부에 대한 사용자 확인. 구현·검토·로컬 통합의 필수 작업은 없습니다.
- 다음 시작: 3337의 새 화면을 사용자와 검토하고, 기존 서버 교체 승인을 받으면 서비스 묶음의 상태를 확인한 뒤 통제된 재시작을 진행합니다.
